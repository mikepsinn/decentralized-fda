# Database Operations Toolkit Configuration

# MySQL Database URL (required for mysql-query.ts and sync)
# Format: mysql://user:password@host:port/database
MYSQL_DATABASE_URL=mysql://root:your_mysql_password@localhost:3306/source_db

# PostgreSQL Database URL (required for postgres-query.ts and sync)
# Format: postgresql://user:password@host:port/database
# You can use either POSTGRES_DATABASE_URL or DATABASE_URL
POSTGRES_DATABASE_URL=postgresql://postgres:your_postgres_password@localhost:5432/target_db
# DATABASE_URL=postgresql://postgres:your_postgres_password@localhost:5432/target_db

# Tables to sync (comma-separated) - only needed for MySQL to PostgreSQL sync
SYNC_TABLES=table1,table2,table3 